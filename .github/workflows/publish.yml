name: Publish

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:
    inputs:
      version:
        description: "Package version (e.g., 1.0.0)"
        required: false
        type: string

jobs:
  # Test with minimal reusable workflow
  test-call:
    uses: ./.github/workflows/test-reusable-callee.yml
    with:
      message: "Testing"
