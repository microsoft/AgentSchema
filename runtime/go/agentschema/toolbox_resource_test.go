// Code generated by AgentSchema emitter; DO NOT EDIT.

package agentschema_test

import (
	"encoding/json"
	"testing"

	"gopkg.in/yaml.v3"

	"github.com/microsoft/agentschema-go/agentschema"
)

// TestToolboxResourceLoadJSON tests loading ToolboxResource from JSON
func TestToolboxResourceLoadJSON(t *testing.T) {
	jsonData := `
{
  "kind": "toolbox",
  "description": "Shared platform tools",
  "tools": [
    {
      "id": "bing_grounding"
    },
    {
      "id": "azure_ai_search",
      "options": {
        "indexName": "products-index"
      }
    },
    {
      "id": "mcp",
      "name": "github-copilot",
      "target": "https://api.githubcopilot.com/mcp",
      "authType": "OAuth2"
    }
  ]
}
`
	var data map[string]interface{}
	if err := json.Unmarshal([]byte(jsonData), &data); err != nil {
		t.Fatalf("Failed to parse JSON: %v", err)
	}

	ctx := agentschema.NewLoadContext()
	instance, err := agentschema.LoadToolboxResource(data, ctx)
	if err != nil {
		t.Fatalf("Failed to load ToolboxResource: %v", err)
	}
	if instance.Kind != "toolbox" {
		t.Errorf(`Expected Kind to be "toolbox", got %v`, instance.Kind)
	}
	if instance.Description == nil || *instance.Description != "Shared platform tools" {
		t.Errorf(`Expected Description to be "Shared platform tools", got %v`, instance.Description)
	}
}

// TestToolboxResourceLoadYAML tests loading ToolboxResource from YAML
func TestToolboxResourceLoadYAML(t *testing.T) {
	yamlData := `
kind: toolbox
description: Shared platform tools
tools:
  - id: bing_grounding
  - id: azure_ai_search
    options:
      indexName: products-index
  - id: mcp
    name: github-copilot
    target: "https://api.githubcopilot.com/mcp"
    authType: OAuth2

`
	var data map[string]interface{}
	if err := yaml.Unmarshal([]byte(yamlData), &data); err != nil {
		t.Fatalf("Failed to parse YAML: %v", err)
	}

	ctx := agentschema.NewLoadContext()
	instance, err := agentschema.LoadToolboxResource(data, ctx)
	if err != nil {
		t.Fatalf("Failed to load ToolboxResource: %v", err)
	}
	if instance.Kind != "toolbox" {
		t.Errorf(`Expected Kind to be "toolbox", got %v`, instance.Kind)
	}
	if instance.Description == nil || *instance.Description != "Shared platform tools" {
		t.Errorf(`Expected Description to be "Shared platform tools", got %v`, instance.Description)
	}
}

// TestToolboxResourceRoundtrip tests load -> save -> load produces equivalent data
func TestToolboxResourceRoundtrip(t *testing.T) {
	jsonData := `
{
  "kind": "toolbox",
  "description": "Shared platform tools",
  "tools": [
    {
      "id": "bing_grounding"
    },
    {
      "id": "azure_ai_search",
      "options": {
        "indexName": "products-index"
      }
    },
    {
      "id": "mcp",
      "name": "github-copilot",
      "target": "https://api.githubcopilot.com/mcp",
      "authType": "OAuth2"
    }
  ]
}
`
	var data map[string]interface{}
	if err := json.Unmarshal([]byte(jsonData), &data); err != nil {
		t.Fatalf("Failed to parse JSON: %v", err)
	}

	loadCtx := agentschema.NewLoadContext()
	instance, err := agentschema.LoadToolboxResource(data, loadCtx)
	if err != nil {
		t.Fatalf("Failed to load ToolboxResource: %v", err)
	}
	saveCtx := agentschema.NewSaveContext()
	savedData := instance.Save(saveCtx)

	reloaded, err := agentschema.LoadToolboxResource(savedData, loadCtx)
	if err != nil {
		t.Fatalf("Failed to reload ToolboxResource: %v", err)
	}
	if reloaded.Kind != "toolbox" {
		t.Errorf(`Expected Kind to be "toolbox", got %v`, reloaded.Kind)
	}
	if reloaded.Description == nil || *reloaded.Description != "Shared platform tools" {
		t.Errorf(`Expected Description to be "Shared platform tools", got %v`, reloaded.Description)
	}
}

// TestToolboxResourceToJSON tests that ToJSON produces valid JSON
func TestToolboxResourceToJSON(t *testing.T) {
	jsonData := `
{
  "kind": "toolbox",
  "description": "Shared platform tools",
  "tools": [
    {
      "id": "bing_grounding"
    },
    {
      "id": "azure_ai_search",
      "options": {
        "indexName": "products-index"
      }
    },
    {
      "id": "mcp",
      "name": "github-copilot",
      "target": "https://api.githubcopilot.com/mcp",
      "authType": "OAuth2"
    }
  ]
}
`
	var data map[string]interface{}
	if err := json.Unmarshal([]byte(jsonData), &data); err != nil {
		t.Fatalf("Failed to parse JSON: %v", err)
	}

	ctx := agentschema.NewLoadContext()
	instance, err := agentschema.LoadToolboxResource(data, ctx)
	if err != nil {
		t.Fatalf("Failed to load ToolboxResource: %v", err)
	}
	jsonOutput, err := instance.ToJSON()
	if err != nil {
		t.Fatalf("Failed to convert to JSON: %v", err)
	}

	var parsed map[string]interface{}
	if err := json.Unmarshal([]byte(jsonOutput), &parsed); err != nil {
		t.Fatalf("Failed to parse generated JSON: %v", err)
	}
}

// TestToolboxResourceToYAML tests that ToYAML produces valid YAML
func TestToolboxResourceToYAML(t *testing.T) {
	jsonData := `
{
  "kind": "toolbox",
  "description": "Shared platform tools",
  "tools": [
    {
      "id": "bing_grounding"
    },
    {
      "id": "azure_ai_search",
      "options": {
        "indexName": "products-index"
      }
    },
    {
      "id": "mcp",
      "name": "github-copilot",
      "target": "https://api.githubcopilot.com/mcp",
      "authType": "OAuth2"
    }
  ]
}
`
	var data map[string]interface{}
	if err := json.Unmarshal([]byte(jsonData), &data); err != nil {
		t.Fatalf("Failed to parse JSON: %v", err)
	}

	ctx := agentschema.NewLoadContext()
	instance, err := agentschema.LoadToolboxResource(data, ctx)
	if err != nil {
		t.Fatalf("Failed to load ToolboxResource: %v", err)
	}
	yamlOutput, err := instance.ToYAML()
	if err != nil {
		t.Fatalf("Failed to convert to YAML: %v", err)
	}

	var parsed map[string]interface{}
	if err := yaml.Unmarshal([]byte(yamlOutput), &parsed); err != nil {
		t.Fatalf("Failed to parse generated YAML: %v", err)
	}
}
