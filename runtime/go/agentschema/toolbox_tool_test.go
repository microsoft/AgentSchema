// Code generated by AgentSchema emitter; DO NOT EDIT.

package agentschema_test

import (
	"encoding/json"
	"testing"

	"gopkg.in/yaml.v3"

	"github.com/microsoft/agentschema-go/agentschema"
)

// TestToolboxToolLoadJSON tests loading ToolboxTool from JSON
func TestToolboxToolLoadJSON(t *testing.T) {
	jsonData := `
{
  "id": "bing_grounding",
  "name": "my-search-tool",
  "target": "https://api.githubcopilot.com/mcp",
  "authType": "OAuth2",
  "options": {
    "indexName": "products-index"
  }
}
`
	var data map[string]interface{}
	if err := json.Unmarshal([]byte(jsonData), &data); err != nil {
		t.Fatalf("Failed to parse JSON: %v", err)
	}

	ctx := agentschema.NewLoadContext()
	instance, err := agentschema.LoadToolboxTool(data, ctx)
	if err != nil {
		t.Fatalf("Failed to load ToolboxTool: %v", err)
	}
	if instance.Id != "bing_grounding" {
		t.Errorf(`Expected Id to be "bing_grounding", got %v`, instance.Id)
	}
	if instance.Name == nil || *instance.Name != "my-search-tool" {
		t.Errorf(`Expected Name to be "my-search-tool", got %v`, instance.Name)
	}
	if instance.Target == nil || *instance.Target != "https://api.githubcopilot.com/mcp" {
		t.Errorf(`Expected Target to be "https://api.githubcopilot.com/mcp", got %v`, instance.Target)
	}
	if instance.AuthType == nil || *instance.AuthType != "OAuth2" {
		t.Errorf(`Expected AuthType to be "OAuth2", got %v`, instance.AuthType)
	}
}

// TestToolboxToolLoadYAML tests loading ToolboxTool from YAML
func TestToolboxToolLoadYAML(t *testing.T) {
	yamlData := `
id: bing_grounding
name: my-search-tool
target: "https://api.githubcopilot.com/mcp"
authType: OAuth2
options:
  indexName: products-index

`
	var data map[string]interface{}
	if err := yaml.Unmarshal([]byte(yamlData), &data); err != nil {
		t.Fatalf("Failed to parse YAML: %v", err)
	}

	ctx := agentschema.NewLoadContext()
	instance, err := agentschema.LoadToolboxTool(data, ctx)
	if err != nil {
		t.Fatalf("Failed to load ToolboxTool: %v", err)
	}
	if instance.Id != "bing_grounding" {
		t.Errorf(`Expected Id to be "bing_grounding", got %v`, instance.Id)
	}
	if instance.Name == nil || *instance.Name != "my-search-tool" {
		t.Errorf(`Expected Name to be "my-search-tool", got %v`, instance.Name)
	}
	if instance.Target == nil || *instance.Target != "https://api.githubcopilot.com/mcp" {
		t.Errorf(`Expected Target to be "https://api.githubcopilot.com/mcp", got %v`, instance.Target)
	}
	if instance.AuthType == nil || *instance.AuthType != "OAuth2" {
		t.Errorf(`Expected AuthType to be "OAuth2", got %v`, instance.AuthType)
	}
}

// TestToolboxToolRoundtrip tests load -> save -> load produces equivalent data
func TestToolboxToolRoundtrip(t *testing.T) {
	jsonData := `
{
  "id": "bing_grounding",
  "name": "my-search-tool",
  "target": "https://api.githubcopilot.com/mcp",
  "authType": "OAuth2",
  "options": {
    "indexName": "products-index"
  }
}
`
	var data map[string]interface{}
	if err := json.Unmarshal([]byte(jsonData), &data); err != nil {
		t.Fatalf("Failed to parse JSON: %v", err)
	}

	loadCtx := agentschema.NewLoadContext()
	instance, err := agentschema.LoadToolboxTool(data, loadCtx)
	if err != nil {
		t.Fatalf("Failed to load ToolboxTool: %v", err)
	}
	saveCtx := agentschema.NewSaveContext()
	savedData := instance.Save(saveCtx)

	reloaded, err := agentschema.LoadToolboxTool(savedData, loadCtx)
	if err != nil {
		t.Fatalf("Failed to reload ToolboxTool: %v", err)
	}
	if reloaded.Id != "bing_grounding" {
		t.Errorf(`Expected Id to be "bing_grounding", got %v`, reloaded.Id)
	}
	if reloaded.Name == nil || *reloaded.Name != "my-search-tool" {
		t.Errorf(`Expected Name to be "my-search-tool", got %v`, reloaded.Name)
	}
	if reloaded.Target == nil || *reloaded.Target != "https://api.githubcopilot.com/mcp" {
		t.Errorf(`Expected Target to be "https://api.githubcopilot.com/mcp", got %v`, reloaded.Target)
	}
	if reloaded.AuthType == nil || *reloaded.AuthType != "OAuth2" {
		t.Errorf(`Expected AuthType to be "OAuth2", got %v`, reloaded.AuthType)
	}
}

// TestToolboxToolToJSON tests that ToJSON produces valid JSON
func TestToolboxToolToJSON(t *testing.T) {
	jsonData := `
{
  "id": "bing_grounding",
  "name": "my-search-tool",
  "target": "https://api.githubcopilot.com/mcp",
  "authType": "OAuth2",
  "options": {
    "indexName": "products-index"
  }
}
`
	var data map[string]interface{}
	if err := json.Unmarshal([]byte(jsonData), &data); err != nil {
		t.Fatalf("Failed to parse JSON: %v", err)
	}

	ctx := agentschema.NewLoadContext()
	instance, err := agentschema.LoadToolboxTool(data, ctx)
	if err != nil {
		t.Fatalf("Failed to load ToolboxTool: %v", err)
	}
	jsonOutput, err := instance.ToJSON()
	if err != nil {
		t.Fatalf("Failed to convert to JSON: %v", err)
	}

	var parsed map[string]interface{}
	if err := json.Unmarshal([]byte(jsonOutput), &parsed); err != nil {
		t.Fatalf("Failed to parse generated JSON: %v", err)
	}
}

// TestToolboxToolToYAML tests that ToYAML produces valid YAML
func TestToolboxToolToYAML(t *testing.T) {
	jsonData := `
{
  "id": "bing_grounding",
  "name": "my-search-tool",
  "target": "https://api.githubcopilot.com/mcp",
  "authType": "OAuth2",
  "options": {
    "indexName": "products-index"
  }
}
`
	var data map[string]interface{}
	if err := json.Unmarshal([]byte(jsonData), &data); err != nil {
		t.Fatalf("Failed to parse JSON: %v", err)
	}

	ctx := agentschema.NewLoadContext()
	instance, err := agentschema.LoadToolboxTool(data, ctx)
	if err != nil {
		t.Fatalf("Failed to load ToolboxTool: %v", err)
	}
	yamlOutput, err := instance.ToYAML()
	if err != nil {
		t.Fatalf("Failed to convert to YAML: %v", err)
	}

	var parsed map[string]interface{}
	if err := yaml.Unmarshal([]byte(yamlOutput), &parsed); err != nil {
		t.Fatalf("Failed to parse generated YAML: %v", err)
	}
}
