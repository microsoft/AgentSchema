// Copyright (c) Microsoft. All rights reserved.
// WARNING: This is an auto-generated file. DO NOT EDIT THIS FILE DIRECTLY.

package agentschema_test

import (
	"encoding/json"
	"testing"

	"gopkg.in/yaml.v3"

	"github.com/microsoft/agentschema-go/agentschema"
)

// TestPropertyLoadJSON tests loading Property from JSON
func TestPropertyLoadJSON(t *testing.T) {
	jsonData := `
{
  "name": "my-input",
  "kind": "string",
  "description": "A description of the input property",
  "required": true,
  "default": "default value",
  "example": "example value",
  "enumValues": [
    "value1",
    "value2",
    "value3"
  ]
}
`
	var data map[string]interface{}
	if err := json.Unmarshal([]byte(jsonData), &data); err != nil {
		t.Fatalf("Failed to parse JSON: %v", err)
	}

	ctx := agentschema.NewLoadContext()
	instance, err := agentschema.LoadProperty(data, ctx)
	if err != nil {
		t.Fatalf("Failed to load Property: %v", err)
	}
	// Polymorphic types return interface{}, extract common fields via reflection or type-specific access
	_ = instance // Load succeeded, exact type depends on discriminator
	// Note: Validation skipped for polymorphic base types - test child types directly
}

// TestPropertyLoadYAML tests loading Property from YAML
func TestPropertyLoadYAML(t *testing.T) {
	yamlData := `
name: my-input
kind: string
description: A description of the input property
required: true
default: default value
example: example value
enumValues:
  - value1
  - value2
  - value3

`
	var data map[string]interface{}
	if err := yaml.Unmarshal([]byte(yamlData), &data); err != nil {
		t.Fatalf("Failed to parse YAML: %v", err)
	}

	ctx := agentschema.NewLoadContext()
	instance, err := agentschema.LoadProperty(data, ctx)
	if err != nil {
		t.Fatalf("Failed to load Property: %v", err)
	}
	// Polymorphic types return interface{}, extract common fields via reflection or type-specific access
	_ = instance // Load succeeded, exact type depends on discriminator
	// Note: Validation skipped for polymorphic base types - test child types directly
}

// TestPropertyRoundtrip tests load -> save -> load produces equivalent data
func TestPropertyRoundtrip(t *testing.T) {
	jsonData := `
{
  "name": "my-input",
  "kind": "string",
  "description": "A description of the input property",
  "required": true,
  "default": "default value",
  "example": "example value",
  "enumValues": [
    "value1",
    "value2",
    "value3"
  ]
}
`
	var data map[string]interface{}
	if err := json.Unmarshal([]byte(jsonData), &data); err != nil {
		t.Fatalf("Failed to parse JSON: %v", err)
	}

	loadCtx := agentschema.NewLoadContext()
	instance, err := agentschema.LoadProperty(data, loadCtx)
	if err != nil {
		t.Fatalf("Failed to load Property: %v", err)
	}
	// Polymorphic roundtrip testing requires type-specific handling
	_ = instance // Load succeeded, exact type depends on discriminator
	// Note: Roundtrip test skipped for polymorphic base types - test child types directly
}

// TestPropertyRoundtripYAML tests YAML load -> save -> load produces equivalent data
func TestPropertyRoundtripYAML(t *testing.T) {
	yamlData := `
name: my-input
kind: string
description: A description of the input property
required: true
default: default value
example: example value
enumValues:
  - value1
  - value2
  - value3

`
	var data map[string]interface{}
	if err := yaml.Unmarshal([]byte(yamlData), &data); err != nil {
		t.Fatalf("Failed to parse YAML: %v", err)
	}

	loadCtx := agentschema.NewLoadContext()
	instance, err := agentschema.LoadProperty(data, loadCtx)
	if err != nil {
		t.Fatalf("Failed to load Property: %v", err)
	}
	// Polymorphic roundtrip testing requires type-specific handling
	_ = instance // Load succeeded, exact type depends on discriminator
	// Note: YAML Roundtrip test skipped for polymorphic base types - test child types directly
}

// TestPropertyToJSON tests that ToJSON produces valid JSON
func TestPropertyToJSON(t *testing.T) {
	jsonData := `
{
  "name": "my-input",
  "kind": "string",
  "description": "A description of the input property",
  "required": true,
  "default": "default value",
  "example": "example value",
  "enumValues": [
    "value1",
    "value2",
    "value3"
  ]
}
`
	var data map[string]interface{}
	if err := json.Unmarshal([]byte(jsonData), &data); err != nil {
		t.Fatalf("Failed to parse JSON: %v", err)
	}

	ctx := agentschema.NewLoadContext()
	instance, err := agentschema.LoadProperty(data, ctx)
	if err != nil {
		t.Fatalf("Failed to load Property: %v", err)
	}
	// Polymorphic ToJSON requires type-specific handling
	_ = instance // Load succeeded, exact type depends on discriminator
	// Note: ToJSON test skipped for polymorphic base types - test child types directly
}

// TestPropertyToYAML tests that ToYAML produces valid YAML
func TestPropertyToYAML(t *testing.T) {
	jsonData := `
{
  "name": "my-input",
  "kind": "string",
  "description": "A description of the input property",
  "required": true,
  "default": "default value",
  "example": "example value",
  "enumValues": [
    "value1",
    "value2",
    "value3"
  ]
}
`
	var data map[string]interface{}
	if err := json.Unmarshal([]byte(jsonData), &data); err != nil {
		t.Fatalf("Failed to parse JSON: %v", err)
	}

	ctx := agentschema.NewLoadContext()
	instance, err := agentschema.LoadProperty(data, ctx)
	if err != nil {
		t.Fatalf("Failed to load Property: %v", err)
	}
	// Polymorphic ToYAML requires type-specific handling
	_ = instance // Load succeeded, exact type depends on discriminator
	// Note: ToYAML test skipped for polymorphic base types - test child types directly
}

// TestPropertyFromInput tests loading Property from boolean
func TestPropertyFromInput(t *testing.T) {
	ctx := agentschema.NewLoadContext()
	instance, err := agentschema.LoadProperty(false, ctx)
	if err != nil {
		t.Fatalf("Failed to load Property from boolean: %v", err)
	}
	// Polymorphic alternate loading requires type-specific handling
	_ = instance // Load succeeded, exact type depends on discriminator
	// Note: Validation skipped for polymorphic base types - test child types directly
}

// TestPropertyFromInputYAML tests loading Property from boolean via YAML
func TestPropertyFromInputYAML(t *testing.T) {
	yamlData := `false`
	var data interface{}
	if err := yaml.Unmarshal([]byte(yamlData), &data); err != nil {
		t.Fatalf("Failed to parse YAML: %v", err)
	}

	ctx := agentschema.NewLoadContext()
	instance, err := agentschema.LoadProperty(data, ctx)
	if err != nil {
		t.Fatalf("Failed to load Property from boolean YAML: %v", err)
	}
	// Polymorphic alternate loading requires type-specific handling
	_ = instance // Load succeeded, exact type depends on discriminator
	// Note: Validation skipped for polymorphic base types - test child types directly
}

// TestPropertyFromInput2 tests loading Property from float32
func TestPropertyFromInput2(t *testing.T) {
	ctx := agentschema.NewLoadContext()
	instance, err := agentschema.LoadProperty(3.14, ctx)
	if err != nil {
		t.Fatalf("Failed to load Property from float32: %v", err)
	}
	// Polymorphic alternate loading requires type-specific handling
	_ = instance // Load succeeded, exact type depends on discriminator
	// Note: Validation skipped for polymorphic base types - test child types directly
}

// TestPropertyFromInputYAML2 tests loading Property from float32 via YAML
func TestPropertyFromInputYAML2(t *testing.T) {
	yamlData := `3.14`
	var data interface{}
	if err := yaml.Unmarshal([]byte(yamlData), &data); err != nil {
		t.Fatalf("Failed to parse YAML: %v", err)
	}

	ctx := agentschema.NewLoadContext()
	instance, err := agentschema.LoadProperty(data, ctx)
	if err != nil {
		t.Fatalf("Failed to load Property from float32 YAML: %v", err)
	}
	// Polymorphic alternate loading requires type-specific handling
	_ = instance // Load succeeded, exact type depends on discriminator
	// Note: Validation skipped for polymorphic base types - test child types directly
}

// TestPropertyFromInput3 tests loading Property from integer
func TestPropertyFromInput3(t *testing.T) {
	ctx := agentschema.NewLoadContext()
	instance, err := agentschema.LoadProperty(4, ctx)
	if err != nil {
		t.Fatalf("Failed to load Property from integer: %v", err)
	}
	// Polymorphic alternate loading requires type-specific handling
	_ = instance // Load succeeded, exact type depends on discriminator
	// Note: Validation skipped for polymorphic base types - test child types directly
}

// TestPropertyFromInputYAML3 tests loading Property from integer via YAML
func TestPropertyFromInputYAML3(t *testing.T) {
	yamlData := `4`
	var data interface{}
	if err := yaml.Unmarshal([]byte(yamlData), &data); err != nil {
		t.Fatalf("Failed to parse YAML: %v", err)
	}

	ctx := agentschema.NewLoadContext()
	instance, err := agentschema.LoadProperty(data, ctx)
	if err != nil {
		t.Fatalf("Failed to load Property from integer YAML: %v", err)
	}
	// Polymorphic alternate loading requires type-specific handling
	_ = instance // Load succeeded, exact type depends on discriminator
	// Note: Validation skipped for polymorphic base types - test child types directly
}

// TestPropertyFromInput4 tests loading Property from string
func TestPropertyFromInput4(t *testing.T) {
	ctx := agentschema.NewLoadContext()
	instance, err := agentschema.LoadProperty("example", ctx)
	if err != nil {
		t.Fatalf("Failed to load Property from string: %v", err)
	}
	// Polymorphic alternate loading requires type-specific handling
	_ = instance // Load succeeded, exact type depends on discriminator
	// Note: Validation skipped for polymorphic base types - test child types directly
}

// TestPropertyFromInputYAML4 tests loading Property from string via YAML
func TestPropertyFromInputYAML4(t *testing.T) {
	yamlData := `"example"`
	var data interface{}
	if err := yaml.Unmarshal([]byte(yamlData), &data); err != nil {
		t.Fatalf("Failed to parse YAML: %v", err)
	}

	ctx := agentschema.NewLoadContext()
	instance, err := agentschema.LoadProperty(data, ctx)
	if err != nil {
		t.Fatalf("Failed to load Property from string YAML: %v", err)
	}
	// Polymorphic alternate loading requires type-specific handling
	_ = instance // Load succeeded, exact type depends on discriminator
	// Note: Validation skipped for polymorphic base types - test child types directly
}
