// Code generated by AgentSchema emitter; DO NOT EDIT.

use agentschema::ProtocolVersionRecord;

#[test]
fn test_protocol_version_record_load_json() {
    let json = r####"
{
  "protocol": "responses",
  "version": "v0.1.1"
}
"####;
    let result = ProtocolVersionRecord::from_json(json);
    assert!(
        result.is_ok(),
        "Failed to load from JSON: {:?}",
        result.err()
    );
    let instance = result.unwrap();
    assert_eq!(instance.protocol, "responses");
    assert_eq!(instance.version, "v0.1.1");
}

#[test]
fn test_protocol_version_record_load_yaml() {
    let yaml = r####"
protocol: responses
version: v0.1.1

"####;
    let result = ProtocolVersionRecord::from_yaml(yaml);
    assert!(
        result.is_ok(),
        "Failed to load from YAML: {:?}",
        result.err()
    );
    let instance = result.unwrap();
    assert_eq!(instance.protocol, "responses");
    assert_eq!(instance.version, "v0.1.1");
}

#[test]
fn test_protocol_version_record_roundtrip() {
    let json = r####"
{
  "protocol": "responses",
  "version": "v0.1.1"
}
"####;
    let result = ProtocolVersionRecord::from_json(json);
    assert!(result.is_ok(), "Failed to load: {:?}", result.err());
    let instance = result.unwrap();
    let json_output = instance.to_json();
    assert!(
        json_output.is_ok(),
        "Failed to serialize to JSON: {:?}",
        json_output.err()
    );
}
