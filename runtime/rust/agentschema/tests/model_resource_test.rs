// Code generated by AgentSchema emitter; DO NOT EDIT.

use agentschema::ModelResource;

#[test]
fn test_model_resource_load_json() {
    let json = r####"
{
  "kind": "model",
  "id": "gpt-4o"
}
"####;
    let result = ModelResource::from_json(json);
    assert!(
        result.is_ok(),
        "Failed to load from JSON: {:?}",
        result.err()
    );
    let instance = result.unwrap();
    assert_eq!(instance.kind, "model");
    assert_eq!(instance.id, "gpt-4o");
}

#[test]
fn test_model_resource_load_yaml() {
    let yaml = r####"
kind: model
id: gpt-4o

"####;
    let result = ModelResource::from_yaml(yaml);
    assert!(
        result.is_ok(),
        "Failed to load from YAML: {:?}",
        result.err()
    );
    let instance = result.unwrap();
    assert_eq!(instance.kind, "model");
    assert_eq!(instance.id, "gpt-4o");
}

#[test]
fn test_model_resource_roundtrip() {
    let json = r####"
{
  "kind": "model",
  "id": "gpt-4o"
}
"####;
    let result = ModelResource::from_json(json);
    assert!(result.is_ok(), "Failed to load: {:?}", result.err());
    let instance = result.unwrap();
    let json_output = instance.to_json();
    assert!(
        json_output.is_ok(),
        "Failed to serialize to JSON: {:?}",
        json_output.err()
    );
}
