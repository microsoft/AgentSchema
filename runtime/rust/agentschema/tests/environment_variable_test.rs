// Code generated by AgentSchema emitter; DO NOT EDIT.

use agentschema::EnvironmentVariable;

#[test]
fn test_environment_variable_load_json() {
    let json = r####"
{
  "name": "MY_ENV_VAR",
  "value": "my-value"
}
"####;
    let result = EnvironmentVariable::from_json(json);
    assert!(
        result.is_ok(),
        "Failed to load from JSON: {:?}",
        result.err()
    );
    let instance = result.unwrap();
    assert_eq!(instance.name, "MY_ENV_VAR");
    assert_eq!(instance.value, "my-value");
}

#[test]
fn test_environment_variable_load_yaml() {
    let yaml = r####"
name: MY_ENV_VAR
value: my-value

"####;
    let result = EnvironmentVariable::from_yaml(yaml);
    assert!(
        result.is_ok(),
        "Failed to load from YAML: {:?}",
        result.err()
    );
    let instance = result.unwrap();
    assert_eq!(instance.name, "MY_ENV_VAR");
    assert_eq!(instance.value, "my-value");
}

#[test]
fn test_environment_variable_roundtrip() {
    let json = r####"
{
  "name": "MY_ENV_VAR",
  "value": "my-value"
}
"####;
    let result = EnvironmentVariable::from_json(json);
    assert!(result.is_ok(), "Failed to load: {:?}", result.err());
    let instance = result.unwrap();
    let json_output = instance.to_json();
    assert!(
        json_output.is_ok(),
        "Failed to serialize to JSON: {:?}",
        json_output.err()
    );
}
