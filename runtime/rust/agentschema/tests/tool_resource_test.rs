// Code generated by AgentSchema emitter; DO NOT EDIT.

use agentschema::ToolResource;

#[test]
fn test_tool_resource_load_json() {
    let json = r####"
{
  "kind": "tool",
  "id": "web-search",
  "options": {
    "myToolResourceProperty": "myValue"
  }
}
"####;
    let result = ToolResource::from_json(json);
    assert!(
        result.is_ok(),
        "Failed to load from JSON: {:?}",
        result.err()
    );
    let instance = result.unwrap();
    assert_eq!(instance.kind, "tool");
    assert_eq!(instance.id, "web-search");
}

#[test]
fn test_tool_resource_load_yaml() {
    let yaml = r####"
kind: tool
id: web-search
options:
  myToolResourceProperty: myValue

"####;
    let result = ToolResource::from_yaml(yaml);
    assert!(
        result.is_ok(),
        "Failed to load from YAML: {:?}",
        result.err()
    );
    let instance = result.unwrap();
    assert_eq!(instance.kind, "tool");
    assert_eq!(instance.id, "web-search");
}

#[test]
fn test_tool_resource_roundtrip() {
    let json = r####"
{
  "kind": "tool",
  "id": "web-search",
  "options": {
    "myToolResourceProperty": "myValue"
  }
}
"####;
    let result = ToolResource::from_json(json);
    assert!(result.is_ok(), "Failed to load: {:?}", result.err());
    let instance = result.unwrap();
    let json_output = instance.to_json();
    assert!(
        json_output.is_ok(),
        "Failed to serialize to JSON: {:?}",
        json_output.err()
    );
}
