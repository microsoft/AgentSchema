// Code generated by AgentSchema emitter; DO NOT EDIT.

use agentschema::*;

#[test]
fn test_agent_manifest_load_json() {
    let json_data = r####"
{
  "name": "basic-prompt",
  "displayName": "My Basic Prompt",
  "description": "A basic prompt that uses the GPT-3 chat API to answer questions",
  "metadata": {
    "authors": [
      "sethjuarez",
      "jietong"
    ],
    "tags": [
      "example",
      "prompt"
    ]
  },
  "template": {
    "kind": "prompt",
    "model": "{{model_name}}",
    "instructions": "You are a poet named {{agent_name}}. Rhyme all your responses."
  },
  "parameters": {
    "strict": true,
    "properties": [
      {
        "name": "model_name",
        "kind": "string",
        "value": "gpt-4o"
      },
      {
        "name": "agent_name",
        "kind": "string",
        "value": "Research Agent"
      }
    ]
  },
  "resources": {
    "gptModelDeployment": {
      "kind": "model",
      "id": "gpt-4o"
    },
    "webSearchInstance": {
      "kind": "tool",
      "id": "web-search",
      "options": {
        "apiKey": "my-api-key"
      }
    }
  }
}
"####;
    let instance =
        AgentManifest::from_json(json_data).expect("Failed to load AgentManifest from JSON");
    assert_eq!(instance.name, "basic-prompt");
    assert_eq!(instance.display_name, "My Basic Prompt");
    assert_eq!(
        instance.description.as_deref(),
        Some("A basic prompt that uses the GPT-3 chat API to answer questions")
    );
}

#[test]
fn test_agent_manifest_load_yaml() {
    let yaml_data = r####"
name: basic-prompt
displayName: My Basic Prompt
description: A basic prompt that uses the GPT-3 chat API to answer questions
metadata:
  authors:
    - sethjuarez
    - jietong
  tags:
    - example
    - prompt
template:
  kind: prompt
  model: "{{model_name}}"
  instructions: You are a poet named {{agent_name}}. Rhyme all your responses.
parameters:
  strict: true
  properties:
    - name: model_name
      kind: string
      value: gpt-4o
    - name: agent_name
      kind: string
      value: Research Agent
resources:
  gptModelDeployment:
    kind: model
    id: gpt-4o
  webSearchInstance:
    kind: tool
    id: web-search
    options:
      apiKey: my-api-key

"####;
    let instance =
        AgentManifest::from_yaml(yaml_data).expect("Failed to load AgentManifest from YAML");
    assert_eq!(instance.name, "basic-prompt");
    assert_eq!(instance.display_name, "My Basic Prompt");
    assert_eq!(
        instance.description.as_deref(),
        Some("A basic prompt that uses the GPT-3 chat API to answer questions")
    );
}

#[test]
fn test_agent_manifest_roundtrip() {
    let json_data = r####"
{
  "name": "basic-prompt",
  "displayName": "My Basic Prompt",
  "description": "A basic prompt that uses the GPT-3 chat API to answer questions",
  "metadata": {
    "authors": [
      "sethjuarez",
      "jietong"
    ],
    "tags": [
      "example",
      "prompt"
    ]
  },
  "template": {
    "kind": "prompt",
    "model": "{{model_name}}",
    "instructions": "You are a poet named {{agent_name}}. Rhyme all your responses."
  },
  "parameters": {
    "strict": true,
    "properties": [
      {
        "name": "model_name",
        "kind": "string",
        "value": "gpt-4o"
      },
      {
        "name": "agent_name",
        "kind": "string",
        "value": "Research Agent"
      }
    ]
  },
  "resources": {
    "gptModelDeployment": {
      "kind": "model",
      "id": "gpt-4o"
    },
    "webSearchInstance": {
      "kind": "tool",
      "id": "web-search",
      "options": {
        "apiKey": "my-api-key"
      }
    }
  }
}
"####;
    let instance = AgentManifest::from_json(json_data).expect("Failed to load AgentManifest");
    let json_out = instance
        .to_json()
        .expect("Failed to serialize AgentManifest to JSON");
    let reloaded =
        AgentManifest::from_json(&json_out).expect("Failed to reload AgentManifest from JSON");
    assert_eq!(reloaded.name, "basic-prompt");
    assert_eq!(reloaded.display_name, "My Basic Prompt");
    assert_eq!(
        reloaded.description.as_deref(),
        Some("A basic prompt that uses the GPT-3 chat API to answer questions")
    );
}

#[test]
fn test_agent_manifest_to_json() {
    let json_data = r####"
{
  "name": "basic-prompt",
  "displayName": "My Basic Prompt",
  "description": "A basic prompt that uses the GPT-3 chat API to answer questions",
  "metadata": {
    "authors": [
      "sethjuarez",
      "jietong"
    ],
    "tags": [
      "example",
      "prompt"
    ]
  },
  "template": {
    "kind": "prompt",
    "model": "{{model_name}}",
    "instructions": "You are a poet named {{agent_name}}. Rhyme all your responses."
  },
  "parameters": {
    "strict": true,
    "properties": [
      {
        "name": "model_name",
        "kind": "string",
        "value": "gpt-4o"
      },
      {
        "name": "agent_name",
        "kind": "string",
        "value": "Research Agent"
      }
    ]
  },
  "resources": {
    "gptModelDeployment": {
      "kind": "model",
      "id": "gpt-4o"
    },
    "webSearchInstance": {
      "kind": "tool",
      "id": "web-search",
      "options": {
        "apiKey": "my-api-key"
      }
    }
  }
}
"####;
    let instance = AgentManifest::from_json(json_data).expect("Failed to load AgentManifest");
    let json_out = instance
        .to_json()
        .expect("Failed to convert AgentManifest to JSON");
    // Verify the output is valid JSON
    let _: serde_json::Value =
        serde_json::from_str(&json_out).expect("Generated JSON is not valid");
}

#[test]
fn test_agent_manifest_to_yaml() {
    let json_data = r####"
{
  "name": "basic-prompt",
  "displayName": "My Basic Prompt",
  "description": "A basic prompt that uses the GPT-3 chat API to answer questions",
  "metadata": {
    "authors": [
      "sethjuarez",
      "jietong"
    ],
    "tags": [
      "example",
      "prompt"
    ]
  },
  "template": {
    "kind": "prompt",
    "model": "{{model_name}}",
    "instructions": "You are a poet named {{agent_name}}. Rhyme all your responses."
  },
  "parameters": {
    "strict": true,
    "properties": [
      {
        "name": "model_name",
        "kind": "string",
        "value": "gpt-4o"
      },
      {
        "name": "agent_name",
        "kind": "string",
        "value": "Research Agent"
      }
    ]
  },
  "resources": {
    "gptModelDeployment": {
      "kind": "model",
      "id": "gpt-4o"
    },
    "webSearchInstance": {
      "kind": "tool",
      "id": "web-search",
      "options": {
        "apiKey": "my-api-key"
      }
    }
  }
}
"####;
    let instance = AgentManifest::from_json(json_data).expect("Failed to load AgentManifest");
    let yaml_out = instance
        .to_yaml()
        .expect("Failed to convert AgentManifest to YAML");
    // Verify the output is valid YAML by re-loading
    let _: serde_json::Value =
        serde_yaml::from_str(&yaml_out).expect("Generated YAML is not valid");
}

#[test]
fn test_agent_manifest_load_json_1() {
    let json_data = r####"
{
  "name": "basic-prompt",
  "displayName": "My Basic Prompt",
  "description": "A basic prompt that uses the GPT-3 chat API to answer questions",
  "metadata": {
    "authors": [
      "sethjuarez",
      "jietong"
    ],
    "tags": [
      "example",
      "prompt"
    ]
  },
  "template": {
    "kind": "prompt",
    "model": "{{model_name}}",
    "instructions": "You are a poet named {{agent_name}}. Rhyme all your responses."
  },
  "parameters": {
    "strict": true,
    "properties": [
      {
        "name": "model_name",
        "kind": "string",
        "value": "gpt-4o"
      },
      {
        "name": "agent_name",
        "kind": "string",
        "value": "Research Agent"
      }
    ]
  },
  "resources": [
    {
      "kind": "model",
      "name": "gptModelDeployment",
      "id": "gpt-4o"
    },
    {
      "kind": "tool",
      "name": "webSearchInstance",
      "id": "web-search",
      "options": {
        "apiKey": "my-api-key"
      }
    }
  ]
}
"####;
    let instance =
        AgentManifest::from_json(json_data).expect("Failed to load AgentManifest from JSON");
    assert_eq!(instance.name, "basic-prompt");
    assert_eq!(instance.display_name, "My Basic Prompt");
    assert_eq!(
        instance.description.as_deref(),
        Some("A basic prompt that uses the GPT-3 chat API to answer questions")
    );
}

#[test]
fn test_agent_manifest_load_yaml_1() {
    let yaml_data = r####"
name: basic-prompt
displayName: My Basic Prompt
description: A basic prompt that uses the GPT-3 chat API to answer questions
metadata:
  authors:
    - sethjuarez
    - jietong
  tags:
    - example
    - prompt
template:
  kind: prompt
  model: "{{model_name}}"
  instructions: You are a poet named {{agent_name}}. Rhyme all your responses.
parameters:
  strict: true
  properties:
    - name: model_name
      kind: string
      value: gpt-4o
    - name: agent_name
      kind: string
      value: Research Agent
resources:
  - kind: model
    name: gptModelDeployment
    id: gpt-4o
  - kind: tool
    name: webSearchInstance
    id: web-search
    options:
      apiKey: my-api-key

"####;
    let instance =
        AgentManifest::from_yaml(yaml_data).expect("Failed to load AgentManifest from YAML");
    assert_eq!(instance.name, "basic-prompt");
    assert_eq!(instance.display_name, "My Basic Prompt");
    assert_eq!(
        instance.description.as_deref(),
        Some("A basic prompt that uses the GPT-3 chat API to answer questions")
    );
}

#[test]
fn test_agent_manifest_roundtrip_1() {
    let json_data = r####"
{
  "name": "basic-prompt",
  "displayName": "My Basic Prompt",
  "description": "A basic prompt that uses the GPT-3 chat API to answer questions",
  "metadata": {
    "authors": [
      "sethjuarez",
      "jietong"
    ],
    "tags": [
      "example",
      "prompt"
    ]
  },
  "template": {
    "kind": "prompt",
    "model": "{{model_name}}",
    "instructions": "You are a poet named {{agent_name}}. Rhyme all your responses."
  },
  "parameters": {
    "strict": true,
    "properties": [
      {
        "name": "model_name",
        "kind": "string",
        "value": "gpt-4o"
      },
      {
        "name": "agent_name",
        "kind": "string",
        "value": "Research Agent"
      }
    ]
  },
  "resources": [
    {
      "kind": "model",
      "name": "gptModelDeployment",
      "id": "gpt-4o"
    },
    {
      "kind": "tool",
      "name": "webSearchInstance",
      "id": "web-search",
      "options": {
        "apiKey": "my-api-key"
      }
    }
  ]
}
"####;
    let instance = AgentManifest::from_json(json_data).expect("Failed to load AgentManifest");
    let json_out = instance
        .to_json()
        .expect("Failed to serialize AgentManifest to JSON");
    let reloaded =
        AgentManifest::from_json(&json_out).expect("Failed to reload AgentManifest from JSON");
    assert_eq!(reloaded.name, "basic-prompt");
    assert_eq!(reloaded.display_name, "My Basic Prompt");
    assert_eq!(
        reloaded.description.as_deref(),
        Some("A basic prompt that uses the GPT-3 chat API to answer questions")
    );
}

#[test]
fn test_agent_manifest_to_json_1() {
    let json_data = r####"
{
  "name": "basic-prompt",
  "displayName": "My Basic Prompt",
  "description": "A basic prompt that uses the GPT-3 chat API to answer questions",
  "metadata": {
    "authors": [
      "sethjuarez",
      "jietong"
    ],
    "tags": [
      "example",
      "prompt"
    ]
  },
  "template": {
    "kind": "prompt",
    "model": "{{model_name}}",
    "instructions": "You are a poet named {{agent_name}}. Rhyme all your responses."
  },
  "parameters": {
    "strict": true,
    "properties": [
      {
        "name": "model_name",
        "kind": "string",
        "value": "gpt-4o"
      },
      {
        "name": "agent_name",
        "kind": "string",
        "value": "Research Agent"
      }
    ]
  },
  "resources": [
    {
      "kind": "model",
      "name": "gptModelDeployment",
      "id": "gpt-4o"
    },
    {
      "kind": "tool",
      "name": "webSearchInstance",
      "id": "web-search",
      "options": {
        "apiKey": "my-api-key"
      }
    }
  ]
}
"####;
    let instance = AgentManifest::from_json(json_data).expect("Failed to load AgentManifest");
    let json_out = instance
        .to_json()
        .expect("Failed to convert AgentManifest to JSON");
    // Verify the output is valid JSON
    let _: serde_json::Value =
        serde_json::from_str(&json_out).expect("Generated JSON is not valid");
}

#[test]
fn test_agent_manifest_to_yaml_1() {
    let json_data = r####"
{
  "name": "basic-prompt",
  "displayName": "My Basic Prompt",
  "description": "A basic prompt that uses the GPT-3 chat API to answer questions",
  "metadata": {
    "authors": [
      "sethjuarez",
      "jietong"
    ],
    "tags": [
      "example",
      "prompt"
    ]
  },
  "template": {
    "kind": "prompt",
    "model": "{{model_name}}",
    "instructions": "You are a poet named {{agent_name}}. Rhyme all your responses."
  },
  "parameters": {
    "strict": true,
    "properties": [
      {
        "name": "model_name",
        "kind": "string",
        "value": "gpt-4o"
      },
      {
        "name": "agent_name",
        "kind": "string",
        "value": "Research Agent"
      }
    ]
  },
  "resources": [
    {
      "kind": "model",
      "name": "gptModelDeployment",
      "id": "gpt-4o"
    },
    {
      "kind": "tool",
      "name": "webSearchInstance",
      "id": "web-search",
      "options": {
        "apiKey": "my-api-key"
      }
    }
  ]
}
"####;
    let instance = AgentManifest::from_json(json_data).expect("Failed to load AgentManifest");
    let yaml_out = instance
        .to_yaml()
        .expect("Failed to convert AgentManifest to YAML");
    // Verify the output is valid YAML by re-loading
    let _: serde_json::Value =
        serde_yaml::from_str(&yaml_out).expect("Generated YAML is not valid");
}
