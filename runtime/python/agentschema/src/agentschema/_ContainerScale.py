##########################################
# WARNING: This is an auto-generated file.
# DO NOT EDIT THIS FILE DIRECTLY
# ANY EDITS WILL BE LOST
##########################################

from dataclasses import dataclass, field
from typing import Any, ClassVar, Optional

from ._context import LoadContext, SaveContext


@dataclass
class ContainerScale:
    """Scaling configuration for a containerized agent.

    Attributes
    ----------
    minReplicas : int
        Minimum number of replicas
    maxReplicas : int
        Maximum number of replicas
    """

    _shorthand_property: ClassVar[Optional[str]] = None

    minReplicas: int = field(default=0)
    maxReplicas: int = field(default=0)

    @staticmethod
    def load(data: Any, context: Optional[LoadContext] = None) -> "ContainerScale":
        """Load a ContainerScale instance.
        Args:
            data (Any): The data to load the instance from.
            context (Optional[LoadContext]): Optional context with pre/post processing callbacks.
        Returns:
            ContainerScale: The loaded ContainerScale instance.

        """

        if context is not None:
            data = context.process_input(data)

        if not isinstance(data, dict):
            raise ValueError(f"Invalid data for ContainerScale: {data}")

        # create new instance
        instance = ContainerScale()

        if data is not None and "minReplicas" in data:
            instance.minReplicas = data["minReplicas"]
        if data is not None and "maxReplicas" in data:
            instance.maxReplicas = data["maxReplicas"]
        if context is not None:
            instance = context.process_output(instance)
        return instance

    def save(self, context: Optional[SaveContext] = None) -> dict[str, Any]:
        """Save the ContainerScale instance to a dictionary.
        Args:
            context (Optional[SaveContext]): Optional context with pre/post processing callbacks.
        Returns:
            dict[str, Any]: The dictionary representation of this instance.

        """
        obj = self
        if context is not None:
            obj = context.process_object(obj)

        result: dict[str, Any] = {}

        if obj.minReplicas is not None:
            result["minReplicas"] = obj.minReplicas
        if obj.maxReplicas is not None:
            result["maxReplicas"] = obj.maxReplicas

        if context is not None:
            result = context.process_dict(result)
        return result

    def to_yaml(self, context: Optional[SaveContext] = None) -> str:
        """Convert the ContainerScale instance to a YAML string.
        Args:
            context (Optional[SaveContext]): Optional context with pre/post processing callbacks.
        Returns:
            str: The YAML string representation of this instance.

        """
        if context is None:
            context = SaveContext()
        return context.to_yaml(self.save(context))

    def to_json(self, context: Optional[SaveContext] = None, indent: int = 2) -> str:
        """Convert the ContainerScale instance to a JSON string.
        Args:
            context (Optional[SaveContext]): Optional context with pre/post processing callbacks.
            indent (int): Number of spaces for indentation. Defaults to 2.
        Returns:
            str: The JSON string representation of this instance.

        """
        if context is None:
            context = SaveContext()
        return context.to_json(self.save(context), indent)
