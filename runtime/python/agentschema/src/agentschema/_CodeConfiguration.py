##########################################
# WARNING: This is an auto-generated file.
# DO NOT EDIT THIS FILE DIRECTLY
# ANY EDITS WILL BE LOST
##########################################

from dataclasses import dataclass, field
from typing import Any, ClassVar, Optional

from ._context import LoadContext, SaveContext


@dataclass
class CodeConfiguration:
    """Configuration for code-based (ZIP upload) deployment of a hosted agent.
    When present, the agent source code is uploaded directly instead of building a container image.

    Attributes
    ----------
    runtime : str
        Runtime identifier for code execution (e.g., 'python_3_11', 'dotnet_8').
    entryPoint : str
        The entry point file for the agent (e.g., 'main.py' for Python, 'HelloWorld.dll' for .NET).
    dependencyResolution : Optional[str]
        How package dependencies are resolved at deployment time.
    """

    _shorthand_property: ClassVar[Optional[str]] = None

    runtime: str = field(default="")
    entryPoint: str = field(default="")
    dependencyResolution: Optional[str] = None

    @staticmethod
    def load(data: Any, context: Optional[LoadContext] = None) -> "CodeConfiguration":
        """Load a CodeConfiguration instance.
        Args:
            data (Any): The data to load the instance from.
            context (Optional[LoadContext]): Optional context with pre/post processing callbacks.
        Returns:
            CodeConfiguration: The loaded CodeConfiguration instance.

        """

        if context is not None:
            data = context.process_input(data)

        if not isinstance(data, dict):
            raise ValueError(f"Invalid data for CodeConfiguration: {data}")

        # create new instance
        instance = CodeConfiguration()

        if data is not None and "runtime" in data:
            instance.runtime = data["runtime"]
        if data is not None and "entryPoint" in data:
            instance.entryPoint = data["entryPoint"]
        if data is not None and "dependencyResolution" in data:
            instance.dependencyResolution = data["dependencyResolution"]
        if context is not None:
            instance = context.process_output(instance)
        return instance

    def save(self, context: Optional[SaveContext] = None) -> dict[str, Any]:
        """Save the CodeConfiguration instance to a dictionary.
        Args:
            context (Optional[SaveContext]): Optional context with pre/post processing callbacks.
        Returns:
            dict[str, Any]: The dictionary representation of this instance.

        """
        obj = self
        if context is not None:
            obj = context.process_object(obj)

        result: dict[str, Any] = {}

        if obj.runtime is not None:
            result["runtime"] = obj.runtime
        if obj.entryPoint is not None:
            result["entryPoint"] = obj.entryPoint
        if obj.dependencyResolution is not None:
            result["dependencyResolution"] = obj.dependencyResolution

        if context is not None:
            result = context.process_dict(result)
        return result

    def to_yaml(self, context: Optional[SaveContext] = None) -> str:
        """Convert the CodeConfiguration instance to a YAML string.
        Args:
            context (Optional[SaveContext]): Optional context with pre/post processing callbacks.
        Returns:
            str: The YAML string representation of this instance.

        """
        if context is None:
            context = SaveContext()
        return context.to_yaml(self.save(context))

    def to_json(self, context: Optional[SaveContext] = None, indent: int = 2) -> str:
        """Convert the CodeConfiguration instance to a JSON string.
        Args:
            context (Optional[SaveContext]): Optional context with pre/post processing callbacks.
            indent (int): Number of spaces for indentation. Defaults to 2.
        Returns:
            str: The JSON string representation of this instance.

        """
        if context is None:
            context = SaveContext()
        return context.to_json(self.save(context), indent)
