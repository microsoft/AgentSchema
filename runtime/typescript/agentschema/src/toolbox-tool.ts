// Copyright (c) Microsoft. All rights reserved.
// WARNING: This is an auto-generated file. DO NOT EDIT THIS FILE DIRECTLY.

import { LoadContext, SaveContext } from "./context";

/**
 * Represents a tool definition within a toolbox.
 * Tools can be Foundry-hosted (web_search, azure_ai_search, etc.)
 * or external (mcp, openapi, a2a_preview) with connection details.
 *
 */
export class ToolboxTool {
  /**
   * The shorthand property name for this type, if any.
   */
  static readonly shorthandProperty: string | undefined = undefined;

  /**
   * The tool type identifier (e.g., 'web_search', 'azure_ai_search', 'mcp', 'a2a_preview')
   */
  id: string = "";

  /**
   * Optional display name for the tool
   */
  name?: string | undefined;

  /**
   * Human-readable description of the tool's capabilities
   */
  description?: string | undefined;

  /**
   * Target endpoint URL for external tools (e.g., MCP server URL, A2A agent URL)
   */
  target?: string | undefined;

  /**
   * Authentication type for the tool connection
   */
  authType?: string | undefined;

  /**
   * Additional configuration options for the tool
   */
  options?: Record<string, unknown> | undefined = {};

  /**
   * Initializes a new instance of ToolboxTool.
   */
  constructor(init?: Partial<ToolboxTool>) {
    this.id = init?.id ?? "";

    if (init?.name !== undefined) {
      this.name = init.name;
    }

    if (init?.description !== undefined) {
      this.description = init.description;
    }

    if (init?.target !== undefined) {
      this.target = init.target;
    }

    if (init?.authType !== undefined) {
      this.authType = init.authType;
    }

    if (init?.options !== undefined) {
      this.options = init.options;
    }
  }

  //#region Load Methods

  /**
   * Load a ToolboxTool instance from a dictionary.
   * @param data - The dictionary containing the data.
   * @param context - Optional context with pre/post processing callbacks.
   * @returns The loaded ToolboxTool instance.
   */
  static load(data: Record<string, unknown>, context?: LoadContext): ToolboxTool {
    if (context) {
      data = context.processInput(data);
    }

    // Create new instance
    const instance = new ToolboxTool();

    if (data["id"] !== undefined && data["id"] !== null) {
      instance.id = String(data["id"]);
    }

    if (data["name"] !== undefined && data["name"] !== null) {
      instance.name = String(data["name"]);
    }

    if (data["description"] !== undefined && data["description"] !== null) {
      instance.description = String(data["description"]);
    }

    if (data["target"] !== undefined && data["target"] !== null) {
      instance.target = String(data["target"]);
    }

    if (data["authType"] !== undefined && data["authType"] !== null) {
      instance.authType = String(data["authType"]);
    }

    if (data["options"] !== undefined && data["options"] !== null) {
      instance.options = data["options"] as Record<string, unknown>;
    }

    if (context) {
      return context.processOutput(instance) as ToolboxTool;
    }
    return instance;
  }

  //#endregion

  //#region Save Methods

  /**
   * Save the ToolboxTool instance to a dictionary.
   * @param context - Optional context with pre/post processing callbacks.
   * @returns The dictionary representation of this instance.
   */
  save(context?: SaveContext): Record<string, unknown> {
    const obj = context ? (context.processObject(this) as ToolboxTool) : this;

    const result: Record<string, unknown> = {};

    if (obj.id !== undefined && obj.id !== null) {
      result["id"] = obj.id;
    }

    if (obj.name !== undefined && obj.name !== null) {
      result["name"] = obj.name;
    }

    if (obj.description !== undefined && obj.description !== null) {
      result["description"] = obj.description;
    }

    if (obj.target !== undefined && obj.target !== null) {
      result["target"] = obj.target;
    }

    if (obj.authType !== undefined && obj.authType !== null) {
      result["authType"] = obj.authType;
    }

    if (obj.options !== undefined && obj.options !== null) {
      result["options"] = obj.options;
    }

    if (context) {
      return context.processDict(result);
    }

    return result;
  }

  /**
   * Convert the ToolboxTool instance to a YAML string.
   * @param context - Optional context with pre/post processing callbacks.
   * @returns The YAML string representation of this instance.
   */
  toYaml(context?: SaveContext): string {
    context = context ?? new SaveContext();
    return context.toYaml(this.save(context));
  }

  /**
   * Convert the ToolboxTool instance to a JSON string.
   * @param context - Optional context with pre/post processing callbacks.
   * @param indent - Number of spaces for indentation. Defaults to 2.
   * @returns The JSON string representation of this instance.
   */
  toJson(context?: SaveContext, indent: number = 2): string {
    context = context ?? new SaveContext();
    return context.toJson(this.save(context), indent);
  }

  /**
   * Load a ToolboxTool instance from a JSON string.
   * @param json - The JSON string to parse.
   * @param context - Optional context with pre/post processing callbacks.
   * @returns The loaded ToolboxTool instance.
   */
  static fromJson(json: string, context?: LoadContext): ToolboxTool {
    const data = JSON.parse(json);

    return ToolboxTool.load(data as Record<string, unknown>, context);
  }

  /**
   * Load a ToolboxTool instance from a YAML string.
   * @param yaml - The YAML string to parse.
   * @param context - Optional context with pre/post processing callbacks.
   * @returns The loaded ToolboxTool instance.
   */
  static fromYaml(yaml: string, context?: LoadContext): ToolboxTool {
    const { parse } = require("yaml");
    const data = parse(yaml);

    return ToolboxTool.load(data as Record<string, unknown>, context);
  }

  //#endregion
}
