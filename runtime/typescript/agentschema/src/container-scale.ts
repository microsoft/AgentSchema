// Copyright (c) Microsoft. All rights reserved.
// WARNING: This is an auto-generated file. DO NOT EDIT THIS FILE DIRECTLY.

import { LoadContext, SaveContext } from "./context";

/**
 * Scaling configuration for a containerized agent.
 *
 */
export class ContainerScale {
  /**
   * The shorthand property name for this type, if any.
   */
  static readonly shorthandProperty: string | undefined = undefined;

  /**
   * Minimum number of replicas
   */
  minReplicas: number = 0;

  /**
   * Maximum number of replicas
   */
  maxReplicas: number = 0;

  /**
   * Initializes a new instance of ContainerScale.
   */
  constructor(init?: Partial<ContainerScale>) {
    this.minReplicas = init?.minReplicas ?? 0;

    this.maxReplicas = init?.maxReplicas ?? 0;
  }

  //#region Load Methods

  /**
   * Load a ContainerScale instance from a dictionary.
   * @param data - The dictionary containing the data.
   * @param context - Optional context with pre/post processing callbacks.
   * @returns The loaded ContainerScale instance.
   */
  static load(data: Record<string, unknown>, context?: LoadContext): ContainerScale {
    if (context) {
      data = context.processInput(data);
    }

    // Create new instance
    const instance = new ContainerScale();

    if (data["minReplicas"] !== undefined && data["minReplicas"] !== null) {
      instance.minReplicas = Number(data["minReplicas"]);
    }

    if (data["maxReplicas"] !== undefined && data["maxReplicas"] !== null) {
      instance.maxReplicas = Number(data["maxReplicas"]);
    }

    if (context) {
      return context.processOutput(instance) as ContainerScale;
    }
    return instance;
  }

  //#endregion

  //#region Save Methods

  /**
   * Save the ContainerScale instance to a dictionary.
   * @param context - Optional context with pre/post processing callbacks.
   * @returns The dictionary representation of this instance.
   */
  save(context?: SaveContext): Record<string, unknown> {
    const obj = context ? (context.processObject(this) as ContainerScale) : this;

    const result: Record<string, unknown> = {};

    if (obj.minReplicas !== undefined && obj.minReplicas !== null) {
      result["minReplicas"] = obj.minReplicas;
    }

    if (obj.maxReplicas !== undefined && obj.maxReplicas !== null) {
      result["maxReplicas"] = obj.maxReplicas;
    }

    if (context) {
      return context.processDict(result);
    }

    return result;
  }

  /**
   * Convert the ContainerScale instance to a YAML string.
   * @param context - Optional context with pre/post processing callbacks.
   * @returns The YAML string representation of this instance.
   */
  toYaml(context?: SaveContext): string {
    context = context ?? new SaveContext();
    return context.toYaml(this.save(context));
  }

  /**
   * Convert the ContainerScale instance to a JSON string.
   * @param context - Optional context with pre/post processing callbacks.
   * @param indent - Number of spaces for indentation. Defaults to 2.
   * @returns The JSON string representation of this instance.
   */
  toJson(context?: SaveContext, indent: number = 2): string {
    context = context ?? new SaveContext();
    return context.toJson(this.save(context), indent);
  }

  /**
   * Load a ContainerScale instance from a JSON string.
   * @param json - The JSON string to parse.
   * @param context - Optional context with pre/post processing callbacks.
   * @returns The loaded ContainerScale instance.
   */
  static fromJson(json: string, context?: LoadContext): ContainerScale {
    const data = JSON.parse(json);

    return ContainerScale.load(data as Record<string, unknown>, context);
  }

  /**
   * Load a ContainerScale instance from a YAML string.
   * @param yaml - The YAML string to parse.
   * @param context - Optional context with pre/post processing callbacks.
   * @returns The loaded ContainerScale instance.
   */
  static fromYaml(yaml: string, context?: LoadContext): ContainerScale {
    const { parse } = require("yaml");
    const data = parse(yaml);

    return ContainerScale.load(data as Record<string, unknown>, context);
  }

  //#endregion
}
