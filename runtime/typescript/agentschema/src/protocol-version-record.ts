// Copyright (c) Microsoft. All rights reserved.
// WARNING: This is an auto-generated file. DO NOT EDIT THIS FILE DIRECTLY.

import { LoadContext, SaveContext } from "./context";

/**
 *
 *
 */
export class ProtocolVersionRecord {
  /**
   * The shorthand property name for this type, if any.
   */
  static readonly shorthandProperty: string | undefined = undefined;

  /**
   * The protocol type.
   */
  protocol: string = "";

  /**
   * The version string for the protocol, e.g. 'v0.1.1'.
   */
  version: string = "";

  /**
   * Initializes a new instance of ProtocolVersionRecord.
   */
  constructor(init?: Partial<ProtocolVersionRecord>) {
    this.protocol = init?.protocol ?? "";

    this.version = init?.version ?? "";
  }

  //#region Load Methods

  /**
   * Load a ProtocolVersionRecord instance from a dictionary.
   * @param data - The dictionary containing the data.
   * @param context - Optional context with pre/post processing callbacks.
   * @returns The loaded ProtocolVersionRecord instance.
   */
  static load(data: Record<string, unknown>, context?: LoadContext): ProtocolVersionRecord {
    if (context) {
      data = context.processInput(data);
    }

    // Create new instance
    const instance = new ProtocolVersionRecord();

    if (data["protocol"] !== undefined && data["protocol"] !== null) {
      instance.protocol = String(data["protocol"]);
    }

    if (data["version"] !== undefined && data["version"] !== null) {
      instance.version = String(data["version"]);
    }

    if (context) {
      return context.processOutput(instance) as ProtocolVersionRecord;
    }
    return instance;
  }

  //#endregion

  //#region Save Methods

  /**
   * Save the ProtocolVersionRecord instance to a dictionary.
   * @param context - Optional context with pre/post processing callbacks.
   * @returns The dictionary representation of this instance.
   */
  save(context?: SaveContext): Record<string, unknown> {
    const obj = context ? (context.processObject(this) as ProtocolVersionRecord) : this;

    const result: Record<string, unknown> = {};

    if (obj.protocol !== undefined && obj.protocol !== null) {
      result["protocol"] = obj.protocol;
    }

    if (obj.version !== undefined && obj.version !== null) {
      result["version"] = obj.version;
    }

    if (context) {
      return context.processDict(result);
    }

    return result;
  }

  /**
   * Convert the ProtocolVersionRecord instance to a YAML string.
   * @param context - Optional context with pre/post processing callbacks.
   * @returns The YAML string representation of this instance.
   */
  toYaml(context?: SaveContext): string {
    context = context ?? new SaveContext();
    return context.toYaml(this.save(context));
  }

  /**
   * Convert the ProtocolVersionRecord instance to a JSON string.
   * @param context - Optional context with pre/post processing callbacks.
   * @param indent - Number of spaces for indentation. Defaults to 2.
   * @returns The JSON string representation of this instance.
   */
  toJson(context?: SaveContext, indent: number = 2): string {
    context = context ?? new SaveContext();
    return context.toJson(this.save(context), indent);
  }

  /**
   * Load a ProtocolVersionRecord instance from a JSON string.
   * @param json - The JSON string to parse.
   * @param context - Optional context with pre/post processing callbacks.
   * @returns The loaded ProtocolVersionRecord instance.
   */
  static fromJson(json: string, context?: LoadContext): ProtocolVersionRecord {
    const data = JSON.parse(json);

    return ProtocolVersionRecord.load(data as Record<string, unknown>, context);
  }

  /**
   * Load a ProtocolVersionRecord instance from a YAML string.
   * @param yaml - The YAML string to parse.
   * @param context - Optional context with pre/post processing callbacks.
   * @returns The loaded ProtocolVersionRecord instance.
   */
  static fromYaml(yaml: string, context?: LoadContext): ProtocolVersionRecord {
    const { parse } = require("yaml");
    const data = parse(yaml);

    return ProtocolVersionRecord.load(data as Record<string, unknown>, context);
  }

  //#endregion
}
