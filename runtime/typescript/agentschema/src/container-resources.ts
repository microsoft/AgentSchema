// Copyright (c) Microsoft. All rights reserved.
// WARNING: This is an auto-generated file. DO NOT EDIT THIS FILE DIRECTLY.

import { LoadContext, SaveContext } from "./context";

/**
 * Resource allocation for a containerized agent.
 * Valid CPU and memory pairings depend on the target hosting provider.
 *
 */
export class ContainerResources {
  /**
   * The shorthand property name for this type, if any.
   */
  static readonly shorthandProperty: string | undefined = undefined;

  /**
   * CPU allocation for the container (e.g., '0.5', '1', '2')
   */
  cpu: string = "";

  /**
   * Memory allocation for the container (e.g., '0.5Gi', '2Gi')
   */
  memory: string = "";

  /**
   * Initializes a new instance of ContainerResources.
   */
  constructor(init?: Partial<ContainerResources>) {
    this.cpu = init?.cpu ?? "";

    this.memory = init?.memory ?? "";
  }

  //#region Load Methods

  /**
   * Load a ContainerResources instance from a dictionary.
   * @param data - The dictionary containing the data.
   * @param context - Optional context with pre/post processing callbacks.
   * @returns The loaded ContainerResources instance.
   */
  static load(data: Record<string, unknown>, context?: LoadContext): ContainerResources {
    if (context) {
      data = context.processInput(data);
    }

    // Create new instance
    const instance = new ContainerResources();

    if (data["cpu"] !== undefined && data["cpu"] !== null) {
      instance.cpu = String(data["cpu"]);
    }

    if (data["memory"] !== undefined && data["memory"] !== null) {
      instance.memory = String(data["memory"]);
    }

    if (context) {
      return context.processOutput(instance) as ContainerResources;
    }
    return instance;
  }

  //#endregion

  //#region Save Methods

  /**
   * Save the ContainerResources instance to a dictionary.
   * @param context - Optional context with pre/post processing callbacks.
   * @returns The dictionary representation of this instance.
   */
  save(context?: SaveContext): Record<string, unknown> {
    const obj = context ? (context.processObject(this) as ContainerResources) : this;

    const result: Record<string, unknown> = {};

    if (obj.cpu !== undefined && obj.cpu !== null) {
      result["cpu"] = obj.cpu;
    }

    if (obj.memory !== undefined && obj.memory !== null) {
      result["memory"] = obj.memory;
    }

    if (context) {
      return context.processDict(result);
    }

    return result;
  }

  /**
   * Convert the ContainerResources instance to a YAML string.
   * @param context - Optional context with pre/post processing callbacks.
   * @returns The YAML string representation of this instance.
   */
  toYaml(context?: SaveContext): string {
    context = context ?? new SaveContext();
    return context.toYaml(this.save(context));
  }

  /**
   * Convert the ContainerResources instance to a JSON string.
   * @param context - Optional context with pre/post processing callbacks.
   * @param indent - Number of spaces for indentation. Defaults to 2.
   * @returns The JSON string representation of this instance.
   */
  toJson(context?: SaveContext, indent: number = 2): string {
    context = context ?? new SaveContext();
    return context.toJson(this.save(context), indent);
  }

  /**
   * Load a ContainerResources instance from a JSON string.
   * @param json - The JSON string to parse.
   * @param context - Optional context with pre/post processing callbacks.
   * @returns The loaded ContainerResources instance.
   */
  static fromJson(json: string, context?: LoadContext): ContainerResources {
    const data = JSON.parse(json);

    return ContainerResources.load(data as Record<string, unknown>, context);
  }

  /**
   * Load a ContainerResources instance from a YAML string.
   * @param yaml - The YAML string to parse.
   * @param context - Optional context with pre/post processing callbacks.
   * @returns The loaded ContainerResources instance.
   */
  static fromYaml(yaml: string, context?: LoadContext): ContainerResources {
    const { parse } = require("yaml");
    const data = parse(yaml);

    return ContainerResources.load(data as Record<string, unknown>, context);
  }

  //#endregion
}
