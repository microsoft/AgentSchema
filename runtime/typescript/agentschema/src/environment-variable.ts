// Copyright (c) Microsoft. All rights reserved.
// WARNING: This is an auto-generated file. DO NOT EDIT THIS FILE DIRECTLY.

import { LoadContext, SaveContext } from "./context";

/**
 * Definition for an environment variable used in containerized agents.
 *
 */
export class EnvironmentVariable {
  /**
   * The shorthand property name for this type, if any.
   */
  static readonly shorthandProperty: string | undefined = undefined;

  /**
   * Name of the environment variable
   */
  name: string = "";

  /**
   * Environment variable resolution
   */
  value: string = "";

  /**
   * Initializes a new instance of EnvironmentVariable.
   */
  constructor(init?: Partial<EnvironmentVariable>) {
    this.name = init?.name ?? "";

    this.value = init?.value ?? "";
  }

  //#region Load Methods

  /**
   * Load a EnvironmentVariable instance from a dictionary.
   * @param data - The dictionary containing the data.
   * @param context - Optional context with pre/post processing callbacks.
   * @returns The loaded EnvironmentVariable instance.
   */
  static load(data: Record<string, unknown>, context?: LoadContext): EnvironmentVariable {
    if (context) {
      data = context.processInput(data);
    }

    // Create new instance
    const instance = new EnvironmentVariable();

    if (data["name"] !== undefined && data["name"] !== null) {
      instance.name = String(data["name"]);
    }

    if (data["value"] !== undefined && data["value"] !== null) {
      instance.value = String(data["value"]);
    }

    if (context) {
      return context.processOutput(instance) as EnvironmentVariable;
    }
    return instance;
  }

  //#endregion

  //#region Save Methods

  /**
   * Save the EnvironmentVariable instance to a dictionary.
   * @param context - Optional context with pre/post processing callbacks.
   * @returns The dictionary representation of this instance.
   */
  save(context?: SaveContext): Record<string, unknown> {
    const obj = context ? (context.processObject(this) as EnvironmentVariable) : this;

    const result: Record<string, unknown> = {};

    if (obj.name !== undefined && obj.name !== null) {
      result["name"] = obj.name;
    }

    if (obj.value !== undefined && obj.value !== null) {
      result["value"] = obj.value;
    }

    if (context) {
      return context.processDict(result);
    }

    return result;
  }

  /**
   * Convert the EnvironmentVariable instance to a YAML string.
   * @param context - Optional context with pre/post processing callbacks.
   * @returns The YAML string representation of this instance.
   */
  toYaml(context?: SaveContext): string {
    context = context ?? new SaveContext();
    return context.toYaml(this.save(context));
  }

  /**
   * Convert the EnvironmentVariable instance to a JSON string.
   * @param context - Optional context with pre/post processing callbacks.
   * @param indent - Number of spaces for indentation. Defaults to 2.
   * @returns The JSON string representation of this instance.
   */
  toJson(context?: SaveContext, indent: number = 2): string {
    context = context ?? new SaveContext();
    return context.toJson(this.save(context), indent);
  }

  /**
   * Load a EnvironmentVariable instance from a JSON string.
   * @param json - The JSON string to parse.
   * @param context - Optional context with pre/post processing callbacks.
   * @returns The loaded EnvironmentVariable instance.
   */
  static fromJson(json: string, context?: LoadContext): EnvironmentVariable {
    const data = JSON.parse(json);

    return EnvironmentVariable.load(data as Record<string, unknown>, context);
  }

  /**
   * Load a EnvironmentVariable instance from a YAML string.
   * @param yaml - The YAML string to parse.
   * @param context - Optional context with pre/post processing callbacks.
   * @returns The loaded EnvironmentVariable instance.
   */
  static fromYaml(yaml: string, context?: LoadContext): EnvironmentVariable {
    const { parse } = require("yaml");
    const data = parse(yaml);

    return EnvironmentVariable.load(data as Record<string, unknown>, context);
  }

  //#endregion
}
