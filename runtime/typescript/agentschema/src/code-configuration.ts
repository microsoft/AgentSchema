// Copyright (c) Microsoft. All rights reserved.
// WARNING: This is an auto-generated file. DO NOT EDIT THIS FILE DIRECTLY.

import { LoadContext, SaveContext } from "./context";

/**
 * Configuration for code-based (ZIP upload) deployment of a hosted agent.
 * When present, the agent source code is uploaded directly instead of building a container image.
 *
 */
export class CodeConfiguration {
  /**
   * The shorthand property name for this type, if any.
   */
  static readonly shorthandProperty: string | undefined = undefined;

  /**
   * Runtime identifier for code execution (e.g., 'python_3_11', 'dotnet_8').
   */
  runtime: string = "";

  /**
   * The entry point file for the agent (e.g., 'main.py' for Python, 'HelloWorld.dll' for .NET).
   */
  entryPoint: string = "";

  /**
   * How package dependencies are resolved at deployment time.
   */
  dependencyResolution?: string | undefined;

  /**
   * Initializes a new instance of CodeConfiguration.
   */
  constructor(init?: Partial<CodeConfiguration>) {
    this.runtime = init?.runtime ?? "";

    this.entryPoint = init?.entryPoint ?? "";

    if (init?.dependencyResolution !== undefined) {
      this.dependencyResolution = init.dependencyResolution;
    }
  }

  //#region Load Methods

  /**
   * Load a CodeConfiguration instance from a dictionary.
   * @param data - The dictionary containing the data.
   * @param context - Optional context with pre/post processing callbacks.
   * @returns The loaded CodeConfiguration instance.
   */
  static load(data: Record<string, unknown>, context?: LoadContext): CodeConfiguration {
    if (context) {
      data = context.processInput(data);
    }

    // Create new instance
    const instance = new CodeConfiguration();

    if (data["runtime"] !== undefined && data["runtime"] !== null) {
      instance.runtime = String(data["runtime"]);
    }

    if (data["entryPoint"] !== undefined && data["entryPoint"] !== null) {
      instance.entryPoint = String(data["entryPoint"]);
    }

    if (data["dependencyResolution"] !== undefined && data["dependencyResolution"] !== null) {
      instance.dependencyResolution = String(data["dependencyResolution"]);
    }

    if (context) {
      return context.processOutput(instance) as CodeConfiguration;
    }
    return instance;
  }

  //#endregion

  //#region Save Methods

  /**
   * Save the CodeConfiguration instance to a dictionary.
   * @param context - Optional context with pre/post processing callbacks.
   * @returns The dictionary representation of this instance.
   */
  save(context?: SaveContext): Record<string, unknown> {
    const obj = context ? (context.processObject(this) as CodeConfiguration) : this;

    const result: Record<string, unknown> = {};

    if (obj.runtime !== undefined && obj.runtime !== null) {
      result["runtime"] = obj.runtime;
    }

    if (obj.entryPoint !== undefined && obj.entryPoint !== null) {
      result["entryPoint"] = obj.entryPoint;
    }

    if (obj.dependencyResolution !== undefined && obj.dependencyResolution !== null) {
      result["dependencyResolution"] = obj.dependencyResolution;
    }

    if (context) {
      return context.processDict(result);
    }

    return result;
  }

  /**
   * Convert the CodeConfiguration instance to a YAML string.
   * @param context - Optional context with pre/post processing callbacks.
   * @returns The YAML string representation of this instance.
   */
  toYaml(context?: SaveContext): string {
    context = context ?? new SaveContext();
    return context.toYaml(this.save(context));
  }

  /**
   * Convert the CodeConfiguration instance to a JSON string.
   * @param context - Optional context with pre/post processing callbacks.
   * @param indent - Number of spaces for indentation. Defaults to 2.
   * @returns The JSON string representation of this instance.
   */
  toJson(context?: SaveContext, indent: number = 2): string {
    context = context ?? new SaveContext();
    return context.toJson(this.save(context), indent);
  }

  /**
   * Load a CodeConfiguration instance from a JSON string.
   * @param json - The JSON string to parse.
   * @param context - Optional context with pre/post processing callbacks.
   * @returns The loaded CodeConfiguration instance.
   */
  static fromJson(json: string, context?: LoadContext): CodeConfiguration {
    const data = JSON.parse(json);

    return CodeConfiguration.load(data as Record<string, unknown>, context);
  }

  /**
   * Load a CodeConfiguration instance from a YAML string.
   * @param yaml - The YAML string to parse.
   * @param context - Optional context with pre/post processing callbacks.
   * @returns The loaded CodeConfiguration instance.
   */
  static fromYaml(yaml: string, context?: LoadContext): CodeConfiguration {
    const { parse } = require("yaml");
    const data = parse(yaml);

    return CodeConfiguration.load(data as Record<string, unknown>, context);
  }

  //#endregion
}
